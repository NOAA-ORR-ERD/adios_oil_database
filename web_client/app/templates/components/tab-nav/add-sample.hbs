<BsButton @type="light" class="m-1 btn-sm text-nowrap"
          @onClick={{this.show_dialog}} >
  <FaIcon @icon="plus" />
</BsButton>


{{#if this.dialogVisible}}
  <TabNav::AddSampleDlg
    @oil={{@oil}}
    @close={{this.close_dialog}}
    @submit={{this.submit}}
  />
{{/if}}
