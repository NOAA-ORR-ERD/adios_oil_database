<div class="detail-section">
  <strong>Water Content:</strong>

  {{#if @editable}}
    <Common::RangeValueInput
      @valueTitle="Water Content"
      @valueObject={{@oil}}
      @valueName="water.fraction"
      @valueUnit="%"
      @valuePrecision="2"
      @sampleIndex={{@sampleIndex}}
      @submit={{this.submit}}
    />
  {{else}}
    {{#if this.waterContent}}
      {{value-unit (convert-unit this.waterContent "%") 2}}
    {{else}}
      No Data
    {{/if}}
  {{/if}}

  &nbsp;

</div>
