<div class="detail-section">
  <div class="table-responsive d-flex">
    <strong>Water Content:</strong>

    <Table::ValueUnitField
      @editable={{this.editable}}
      @value={{oil.water.fraction}}
      @units="%"
      @precision={{2}}
      @hasValue={{oil.water}}
    />

    {{!-- {{#if oil.water}}
      {{value-unit (convert-unit oil.water.fraction "%") 2}}
    {{else}}
      No Data
    {{/if}} --}}

  </div>

  &nbsp;

</div>
