<div class="detail-section">
  <strong>Alkylated PAHs:</strong>

  <div class="row">
    <div class="col-xs-6 col-sm-6 col-lg-8">

      <div class="row">

        <div class="col-xs-12 col-lg-6">
          <div class="table-responsive">
            <Table::AlkylatedPah::CxN
              @oil={{this.oil}}
              @editable={{this.editable}}
              @submit={{this.submit}}
            />
          </div>
        </div>

        <div class="col-xs-12 col-lg-6">
          <div class="table-responsive">
            <Table::AlkylatedPah::CxP
              @oil={{this.oil}}
              @editable={{this.editable}}
              @submit={{this.submit}}
            />
          </div>
        </div>

        <div class="col-xs-12 col-lg-6">
          <div class="table-responsive">
            <Table::AlkylatedPah::CxF
              @oil={{this.oil}}
              @editable={{this.editable}}
              @submit={{this.submit}}
            />
          </div>
        </div>

        <div class="col-xs-12 col-lg-6">
          <div class="table-responsive">
            <Table::AlkylatedPah::CxC
              @oil={{this.oil}}
              @editable={{this.editable}}
              @submit={{this.submit}}
            />
          </div>
        </div>

        <div class="col-xs-12 col-lg-6">
          <div class="table-responsive">
            <Table::AlkylatedPah::CxB
              @oil={{this.oil}}
              @editable={{this.editable}}
              @submit={{this.submit}}
            />
          </div>
        </div>

        <div class="col-xs-12 col-lg-6">
          <div class="table-responsive">
            <Table::AlkylatedPah::CxD
              @oil={{this.oil}}
              @editable={{this.editable}}
              @submit={{this.submit}}
            />
          </div>
        </div>

      </div>
    </div>

    <div class="col-xs-6 col-sm-6 col-lg-4">
      <div class="table-responsive">
        <Table::AlkylatedPah::Other
          @oil={{this.oil}}
          @editable={{this.editable}}
          @submit={{this.submit}}
        />
      </div>
    </div>

  </div>
</div>
