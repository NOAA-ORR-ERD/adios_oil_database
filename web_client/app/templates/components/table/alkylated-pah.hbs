<div class="detail-section">
  <strong>Alkylated PAHs:</strong>

  <div class="row">
    <div class="col-xs-6 col-sm-6 col-lg-8">

      <div class="row">

        <div class="col-xs-12 col-lg-4">
          <div class="table-responsive">
            <Common::StaticTable
              @store={{@store}}
              @oil={{@oil}}
              @editable={{@editable}}
              @submit={{@submit}}
              @tableTitle="Naphthalenes"
              @boldTitle={{false}}
              @propertyName="alkylated_pahs"
              @valueUnit="ppm"
              @valuePrecision={{3}}
            />
          </div>
        </div>

        <div class="col-xs-12 col-lg-4">
          <div class="table-responsive">
            <Common::StaticTable
              @store={{@store}}
              @oil={{@oil}}
              @editable={{@editable}}
              @submit={{@submit}}
              @tableTitle="Phenanthrenes"
              @boldTitle={{false}}
              @propertyName="alkylated_pahs"
              @valueUnit="ppm"
              @valuePrecision={{3}}
            />
          </div>
        </div>

        <div class="col-xs-12 col-lg-4">
          <div class="table-responsive">
            <Common::StaticTable
              @store={{@store}}
              @oil={{@oil}}
              @editable={{@editable}}
              @submit={{@submit}}
              @tableTitle="Fluorenes"
              @boldTitle={{false}}
              @propertyName="alkylated_pahs"
              @valueUnit="ppm"
              @valuePrecision={{3}}
            />
          </div>
        </div>

        <div class="col-xs-12 col-lg-4">
          <div class="table-responsive">
            <Common::StaticTable
              @store={{@store}}
              @oil={{@oil}}
              @editable={{@editable}}
              @submit={{@submit}}
              @tableTitle="Chrysenes"
              @boldTitle={{false}}
              @propertyName="alkylated_pahs"
              @valueUnit="ppm"
              @valuePrecision={{3}}
            />
          </div>
        </div>

        <div class="col-xs-12 col-lg-4">
          <div class="table-responsive">
            <Common::StaticTable
              @store={{@store}}
              @oil={{@oil}}
              @editable={{@editable}}
              @submit={{@submit}}
              @tableTitle="Benzonaphthothiophenes"
              @boldTitle={{false}}
              @propertyName="alkylated_pahs"
              @valueUnit="ppm"
              @valuePrecision={{3}}
            />
          </div>
        </div>

        <div class="col-xs-12 col-lg-4">
          <div class="table-responsive">
            <Common::StaticTable
              @store={{@store}}
              @oil={{@oil}}
              @editable={{@editable}}
              @submit={{@submit}}
              @tableTitle="Dibenzothiophenes"
              @boldTitle={{false}}
              @propertyName="alkylated_pahs"
              @valueUnit="ppm"
              @valuePrecision={{3}}
            />
          </div>
        </div>

      </div>
    </div>

    <div class="col-xs-6 col-sm-6 col-lg-4">
      <div class="table-responsive">
        <Common::StaticTable
          @store={{@store}}
          @oil={{@oil}}
          @editable={{@editable}}
          @submit={{@submit}}
          @tableTitle="Other Priority PAHs"
          @boldTitle={{false}}
          @propertyName="alkylated_pahs"
          @valueUnit="ppm"
          @valuePrecision={{3}}
        />
      </div>
    </div>

  </div>
</div>
