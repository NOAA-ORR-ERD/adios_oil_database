<BsButton @type="dark" class="m-1 btn-sm text-nowrap"
          @onClick={{this.show_dialog}} >
  Add New Oil
</BsButton>


{{#if this.dialogVisible}}
  <Dialog::AddNewOil
    @submit={{this.submit}}
    @close={{this.close_dialog}}
  />
{{/if}}
