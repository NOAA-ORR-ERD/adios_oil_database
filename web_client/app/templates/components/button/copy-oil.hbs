<BsButton @type="dark" @onClick={{this.show_dialog}}
          class="m-1 btn-sm text-nowrap" >
  Copy
</BsButton>


{{#if this.dialogVisible}}
  <Dialog::CopyOil
    @oil={{@oil}}
    @submit={{this.submit}}
    @close={{this.close_dialog}}
  />
{{/if}}
