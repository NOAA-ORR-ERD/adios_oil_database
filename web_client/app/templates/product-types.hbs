<strong>Product Types:</strong>

<ul>
  {{#each this.model as |ptypes|}}
    {{log ptypes}}
    {{#each ptypes.product_types as |ptype|}}
      <li>{{ptype}}</li>
    {{/each}}
  {{/each}}
</ul>

{{outlet}}
